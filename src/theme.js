import { css } from "styled-components";

const theme = {
	tipography: {
		h3: css`
		font-weight: 700;
		font-size: 18px;
		line-height: 18px;
		letter-spacing: 4px;
		`,
	},
};

export default theme;
